#!/bin/sh

# Sets and samples the release environment variable.
# The value will be persisted to both stdout and the config file.

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=<%= @release.name %>@127.0.0.1
